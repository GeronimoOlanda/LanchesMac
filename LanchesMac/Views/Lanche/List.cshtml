@model LancheListViewModel
@*  <div>
    Definido na controller(ViewData permite a gente recuperar dados da controller e colocar direto na view)
    @ViewData["Titulo"]

    <h2>@ViewData["Data"]</h2>
    </div>
    <hr />
    <h3>
        @ViewBag.Total @ViewBag.TotalLanches
    </h3>
    <hr />
    @if (TempData.ContainsKey("Nome"))
    {
        <h3>@TempData["Nome"];</h3>
    }


    @{
        //Permite definir um layout para cada view
        Layout = null;
    }
*@

<style>
    *{
        font-family: Arial, Helvetica, sans-serif;
    }
</style>
<div> 
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container">
    <div class="row">
        @foreach (var lanche in Model.lanches)
        {
            //renderizando a nossa partialview, passando o for com a model lanche,estamos enviando os dados para a partial para serem renderizados.
            <partial name="_LanchesResumo", for="@lanche"/>
        }
    </div>
</div>